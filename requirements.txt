# Core implementation uses only the Python standard library.
# Development / test dependency:
pytest>=8.0

# Environment configuration
python-dotenv>=1.0,<2.0

# HTTP API
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0

# API integration tests
httpx>=0.27,<1.0
